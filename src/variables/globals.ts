export const DEV_API: string = "http://localhost:3000/new-mohsaid99";

// export const isAdmin = (role: number, apikey: string): boolean =>
//   role === +import.meta.env.VITE_ADMIN_ROLE &&
//   apikey === import.meta.env.VITE_ADMIN_API;

// export const isMonmon = (role: number, apikey: string): boolean =>
//   role === +import.meta.env.VITE_MONMON_ROLE &&
//   apikey === import.meta.env.VITE_MONMON_API;

// export const isNotLoggedIn = (username: string | null): boolean =>
//   username === null;

// export const isLoggedIn = (username: string | null): boolean =>
//   username !== null;
